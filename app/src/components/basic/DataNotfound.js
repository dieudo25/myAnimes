import React from "react";

const DataNotFound = () => (
    /* Display when the data is not found after fetching from the api */
    
    <span className="material-icons text-main-500">
        cancel
    </span>
);

export default DataNotFound;